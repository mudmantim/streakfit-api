# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtualenvs
.venv/
venv/
env/

# Secrets / local config
.env
.env.*
!.env.example

# Flask instance folder (local SQLite DBs, runtime data)
instance/

# OS / editor
.DS_Store
*.swp

# Exercise-illustration style exploration — kept locally, not shipped
static/exercise_samples/

# Evaluation artifacts — regenerated by a run, never reviewed as source.
# docs/setup/anthropic-key.md already told people the transcript was ignored;
# it was not, so a run left it staged.
coach_eval_results.json
eval_spend.json

# Review packets are derived from the store — regenerate with
# `review_queue.py --emit`. The ledger and the verdicts are evidence and ARE
# tracked: they are the record of who decided what, and why.
content/reviews/packets/
rickie_scenarios_results.json
